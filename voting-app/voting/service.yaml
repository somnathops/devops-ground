apiVersion: v1
kind: Service
metadata:
  name: voting-app
  namespace: voting-dev
spec:
  type: NodePort
  selector:
    app: voting-app  # Send traffic to pods with this label
  ports:
  - port: 80
    targetPort: 80
    nodePort: 31000 